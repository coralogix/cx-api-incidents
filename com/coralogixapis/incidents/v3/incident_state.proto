syntax = "proto3";

package com.coralogixapis.incidents.v3;

enum IncidentState {
  INCIDENT_STATE_TRIGGERED_OR_UNSPECIFIED = 0;
  INCIDENT_STATE_ACKNOWLEDGED = 1;
  INCIDENT_STATE_SNOOZED = 2;
  INCIDENT_STATE_RESOLVED = 3;
  INCIDENT_STATE_CLOSED = 4;
}
